const config = {
  
  // Ship
  shipWidth: 20,
  shipHeight: 100,
  shipSpinSpeed: Math.PI * 2,

  collectorRadius: 90,
  attractorRadius: 75,
  attractorJitterFactor: .5,
  attractorSpeed: 15,

  maxFuel: 70,
  fuelRate: 7.5,

  // Starfield
  starSpeed: 15
};

export default config;